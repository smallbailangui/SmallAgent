[project]
name = "smallagent"
version = "0.1.0"
description = "一个不依赖 agent 框架的最小编程智能体。"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
smallagent = "smallagent.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
